build:
  gpu: true
  python_version: "3.10"
  run:
    - pip install packaging
  python_packages:
    - packaging
    - transformers>=4.39.2
    - torch
    - flash-attn>=2.5.6
    - accelerate
predict: "predict.py:Predictor"
